# SCAN VFX 작업 기록 및 이어받기

작성일: 2026-09-20

## 현재 요청과 상태

20초 힙합 영상에 포토그래메트리 스캔 오류, 독립적인 부위 움직임, 깊이감, 트래킹 그래픽을 합성하는 After Effects 작업이다. 사용자가 직접 편집한 프로젝트와 새로 제공한 렌더 소스를 기준으로 여러 차례 방향을 수정했다.

**최신 저장본은 `outputs/parts-composite/Parts_Composite_Corrected.aep`이다.** `PARTS_COMPOSITE_20s` 컴포지션에서 작업한다. 마지막 수정 스크립트는 인물 10개 레이어가 `FINAL_SCAN_VFX_1.mp4`를 직접 사용하도록 교체했고 저장 완료를 보고했다. 이 수정 직후 사용자가 작업 기록의 GitHub 업로드를 요청했다. **최신 교체 결과의 전체 재생 검수와 최종 영상 출력은 아직 완료하지 않았다.**

현재 단계는 새 영상 생성이 아니라, 사용자가 제공한 두 렌더 영상을 부위별로 합성하는 작업이다.

## 사용자가 원하는 결과

- `_1` 영상의 인물 꿀렁임을 살린다.
- 영상 두 장을 낮은 불투명도로 포개서 이중상처럼 만들지 않는다.
- 얼굴, 머리, 왼손, 오른손, 몸을 각 인물별로 별도 레이어로 취급한다.
- 책상 전체를 덮기보다 모서리·다리 일부, 창문 프레임·유리 일부 등 작은 영역도 부분적으로 합성한다.
- 겹쳐진 조각의 색감은 미세하게 구분하되 자연스럽게 연결한다.
- 인물과 공간을 동일한 변형으로 함께 흔들지 않는다. 부위와 표면마다 움직임 차이가 있어야 한다.
- 스캔된 공간의 찢어진 경계, 빠진 면, 텍스처 오류와 깊이감을 활용한다.
- 이전의 전체 삼각망 화면, 반투명 인물 겹침, 인물 주변의 번진 메움 자국은 거절된 결과다.

## 최신 두 소스

사용자의 Downloads 폴더에 있는 파일이다. 영상 파일 자체는 이 문서에 포함하지 않는다.

| 파일 | 역할 및 특성 |
| --- | --- |
| `FINAL_SCAN_VFX_1.mp4` | 사용자가 선호하는 인물/공간의 꿀렁임과 기존 오류 연출이 들어 있는 소스 |
| `FINAL_SCAN_VFX_20s.mp4` | 상대적으로 정돈된 다른 렌더 소스. 배경과 부분 디테일 합성에 사용 |

두 파일 모두 확인된 사양: **3840×2160, 24fps, 480프레임, 20초**. 같은 길이지만 프레임 내 인물·공간 위치는 서로 다르다.

## 최신 합성 구조

### 첫 합성본: 교체 방향 오류

`Parts_Composite.aep`에서는 `_1`을 베이스로 두고 `_20s`를 광학 흐름으로 `_1`에 정렬한 후 인물 부위별로 얹었다. 사용자가 “인물 교체가 안됐다”고 지적했다. 원하는 인물 소스 선택이 잘못됐으므로 이 파일을 최종본으로 취급하지 않는다.

### 수정본: 인물은 `_1` 원본에서 직접 사용

`Parts_Composite_Corrected.aep`에서 다음을 적용했다.

1. 두 인물의 10개 인물 레이어를 정렬된 `_20s` 소스에서 **원래 `_1` 소스**로 교체했다.
2. `_20s` 배경 레이어를 추가했다.
3. `_1` 인물 주변을 확장한 보호 매트로 `_20s`의 인물 영역을 제외했다.
4. 맨 아래 `_1` 전체 소스를 접합부 보완용으로 남겼다. 따라서 인물 주변의 일부 배경도 `_1`에서 올 수 있다. 깨끗한 배경 복원판으로 완전히 교체한 구조는 아니다.
5. 가구 모서리와 창문 디테일은 정렬된 `_20s` 패치를 유지했다.
6. 각 패치의 불투명도는 100%이며 공간적 매트 경계에만 얇은 부드러움을 줬다. 전체 영상끼리의 반투명 이중 노출 방식은 사용하지 않았다.

각 인물의 부위:

- `left-face`, `right-face`
- `left-head`, `right-head`
- `left-hand-L`, `left-hand-R`
- `right-hand-L`, `right-hand-R`
- `left-body`, `right-body`

추가 디테일:

- `table-edges`
- `window-details`

좌우는 화면상 인물 구분이다. 손의 L/R은 추적 모델의 랜드마크 명칭을 따른다.

## 추적과 매트 생성 방법

`work/vfx/compose-parts.py`가 최신 부위별 소스를 만들었다.

- MediaPipe Pose Landmarker full 모델로 각 인물을 나눠 추적했다.
- 안정 버전 `mediapipe==0.10.21`을 사용했다. 최신 버전은 이 Mac에서 그래픽 서비스 관련 실행 오류가 발생했다.
- 인물별 세그멘테이션과 얼굴/귀/어깨/손목/손가락 랜드마크를 조합했다.
- 얼굴은 타원, 머리는 큰 머리 영역에서 얼굴을 뺀 매트, 손은 손목·손가락 점을 둘러싼 확장 영역이다.
- `body`는 인물 세그멘테이션에서 머리와 손 영역을 뺀 나머지로, 몸통뿐 아니라 다리도 포함한다.
- 검출 성공 프레임 수: 왼쪽 **458/480**, 오른쪽 **449/480**. 검출 실패 프레임에는 직전 유효 매트를 재사용했다.
- 따라서 이는 **자동 추적 기반 초안 매트**이며, 모든 프레임을 수작업 로토스코핑한 결과는 아니다. 컷 부근, 손가락, 모자, 겹침 영역의 추가 검수가 필요하다.
- `_20s`의 정렬 소스는 960×540에서 계산한 DIS optical flow를 4K로 확대해 리맵했다. 비강체 변형이나 가려짐이 심한 곳에서 늘어짐이 생길 수 있다.
- 가구 모서리는 제한된 세로 범위에서 Hough 직선 검출로 추출했다. 창문 디테일은 좌측 상부 영역의 밝은 에지를 추출했다. **정확한 의미 기반 책상/창문 전체 분할은 아니다.** 선반이나 벽의 선이 포함될 수 있다.
- `person-union.py`는 인물 매트 10개를 합치고 팽창·페더 처리해 배경 보호용 매트를 만들었다.

## 최신 파일 목록

경로는 이 작업 폴더 기준이다.

| 경로 | 설명 |
| --- | --- |
| `outputs/parts-composite/Parts_Composite_Corrected.aep` | 최신 인물 소스 교체본 |
| `outputs/parts-composite/Before_person_correction.aep` | 인물 소스 교체 직전 백업 |
| `outputs/parts-composite/Parts_Composite.aep` | 교체 방향이 잘못됐던 첫 부위별 합성본 |
| `outputs/parts-composite/Before_parts_composite.aep` | 사용자가 열어둔 원래 두 레이어 프로젝트의 백업 |
| `outputs/parts-composite/Assets/aligned-detail-source.mp4` | `_1`의 좌표에 맞춘 `_20s` 소스 |
| `outputs/parts-composite/Assets/*-face.mp4` 등 | 얼굴·머리·손·몸 부위 매트 |
| `outputs/parts-composite/Assets/table-edges.mp4` | 가구 에지 매트 |
| `outputs/parts-composite/Assets/window-details.mp4` | 창문/좌측 밝은 에지 매트 |
| `outputs/parts-composite/Assets/person-background-guard.mp4` | 인물 주변 배경 교체 보호 매트 |
| `outputs/parts-composite/tracking-report.json` | 검출 성공 프레임 수 |
| `outputs/parts-composite/replacement-report.txt` | 최신 10개 인물 소스 교체 및 저장 완료 기록 |
| `outputs/parts-composite/masks-*.jpg` | 일부 시점의 매트 확인 이미지 |
| `outputs/parts-composite/person-corrected-01.png`, `person-corrected-05.png` | 교체 스크립트가 요청한 확인 프레임. 최신 작업 중 직접 검수는 미완료 |

AEP는 로컬 영상 파일을 참조한다. **AEP나 이 Markdown만 다른 컴퓨터로 옮겨서는 소스가 모두 따라가지 않는다.** 이어받을 때 원본 두 영상과 Assets 폴더도 확보하고 파일을 재연결해야 한다.

## 핵심 스크립트

| 파일 | 기능 |
| --- | --- |
| `work/vfx/inspect-two.jsx` | 사용자가 열어둔 프로젝트의 두 소스와 레이어 확인 |
| `work/vfx/inspect-pair.py` | 두 소스의 해상도·fps·프레임 수 및 비교 이미지 확인 |
| `work/vfx/compose-parts.py` | 영상 정렬과 인물/디테일별 매트 생성 |
| `work/vfx/assemble-parts.jsx` | 처음 만든 12개 부분 합성 컴포지션 구성 |
| `work/vfx/person-union.py` | 배경 교체 시 인물 주변 보호 매트 생성 |
| `work/vfx/correct-person-source.jsx` | 인물 레이어를 `_1` 소스로 바로잡고 `_20s` 배경 추가 |

JSX는 After Effects의 파일 → 스크립트 → 스크립트 파일 실행으로 실행했다. 경로가 이 작업 컴퓨터에 맞게 작성되어 있으므로 다른 환경에서 그대로 실행하면 안 된다. 생성 스크립트는 중복 실행 시 레이어가 추가될 수 있어 현재 프로젝트와 백업을 확인해야 한다.

## 이전 VFX 작업과 사용자 피드백

이전 작업은 `outputs/scan-vfx/`와 `work/vfx/`에 남아 있다. 현재 부위별 두 영상 합성과 구분한다.

- 원래 영상: `비디오 노드 1 - 사본.mp4`.
- 트래킹 스타일 참고: `baby-track-video.mp4`.
- 사용자가 공급한 7개 패스: Depth, Metallic, Specular, Roughness, Normal, Alpha, BaseColor의 `(1).mp4` 파일.
- 7개 패스는 각각 1920×1080, 24fps, 481프레임, 약 20.04초로 확인했다.
- Alpha는 인물 중심 매트지만 신발 일부가 반투명하게 들어 있어 이후 불투명 매트로 보정했다.
- 바닥·벽·천장·반사면·가구·마이크·조각을 표면 방향과 위치 기반으로 분리해 움직임을 달리한 시도가 있다. 의미 기반 정밀 로토와는 다르다.
- 인물 주변의 배경 메움 흔적과 겹침이 문제로 지적됐다. 인물 주변은 변형을 제한하고 별도 매트로 보호했다.
- 회색 빈 공간에 블루스크린을 넣었다가 사용자가 거절했다.
- CMD 오류 화면, 간헐적 그린스크린+빨간 `+` 표식을 시험했다. 이후 사용자가 배경과 색감을 직전 상태로 돌리고 레이어 분리 움직임만 반영하라고 요청했다.
- 과하게 누른 색감과 추가 Gaussian blur도 되돌리라고 요청받았다.
- 00:00:07:13–00:00:07:14의 끊김은 전경 조각이 지나가며 가리는 방향으로 작업했다.
- 00:00:18:11 이후 전체 왜곡 및 끝→처음 루프 연결을 요청받았다.
- 이후 사용자가 `2_1.aep`를 열어 수정한 상태에서 작업을 이어갔다.
- 전경에 균일한 삼각망을 씌운 결과는 명확히 거절됐다. 이 시도는 채택된 최종 스타일이 아니다.
- 벽의 실제 질감 일부를 점/선으로 변환하는 대안을 생성하기 시작했지만, 사용자가 새로운 두 영상 합성으로 작업을 바꿨다. 대안은 현재 합성에 적용됐다고 주장하지 않는다.

## 참고 링크

- https://www.instagram.com/reel/DcZqvDdMzOg/
- https://www.instagram.com/reel/DdU-DHRKTxK/
- https://www.instagram.com/reel/DbwonWgsC_3/
- https://www.instagram.com/reel/DdHUPRAs5T8/
- https://www.instagram.com/reel/DcbaFTJMDkz/
- https://www.instagram.com/reels/DcWD7TsugAk/

마지막 링크는 브라우저에서 자동차 표면의 포인트 클라우드와 원본을 조합하는 화면을 확인했다. 튜토리얼 전체의 설정값을 완전히 재현한 것은 아니다. 다른 링크들도 모두 끝까지 분석됐다고 가정하지 않는다.

## 다음에 해야 할 검수

1. `Parts_Composite_Corrected.aep`의 `PARTS_COMPOSITE_20s`를 연다.
2. 인물 10개 레이어의 실제 소스가 `_1`인지 확인한다.
3. `_20s`의 배경 인물이 인물 주변 보호 매트 밖으로 새어 나오지 않는지 확인한다.
4. 첫 장면, 손 제스처, 모자·신발, 앉음/서있음 전환, 검출 실패 프레임의 매트를 확인한다.
5. 가구/창문 자동 에지 중 잘못 잡힌 선은 마스크를 수정하거나 제외한다.
6. 전체를 실시간 재생해 입모양과 원래 오디오 타이밍, 부위 경계의 떨림을 확인한다.
7. 사용자가 원한 교체 방향과 겹친 조각의 표현이 맞는지 확인한 후 필요하면 최종 출력한다.

**현재는 프로젝트 저장까지 진행한 상태다. 최종 합성 영상의 전체 검수·출력 완료로 보고하면 안 된다.**
